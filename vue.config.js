const { defineConfig } = require('@vue/cli-service');
module.exports = defineConfig({
  transpileDependencies: true,
  publicPath: '/portfolio/' // Ganti 'nama-repo' dengan nama repositori GitHub Anda
});